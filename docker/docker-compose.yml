version: '3.8'
services:
  mc:
    image: itzg/minecraft-server
    container_name: minecraft-server
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      TYPE: "PAPER"
      MEMORY: "3G"
      VERSION: "1.20.1"
    volumes:
      - /opt/minecraft/data:/data
    restart: always